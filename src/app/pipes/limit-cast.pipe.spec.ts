import { LimitCastPipe } from './limit-cast.pipe';

describe('LimitCastPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitCastPipe();
    expect(pipe).toBeTruthy();
  });
});
